# Base Sepolia webapp configuration

# Network selection
NEXT_PUBLIC_CHAIN_SELECTION=base_sepolia

# Contract addresses (required)
NEXT_PUBLIC_ASTAVERDE_ADDRESS=0x...
NEXT_PUBLIC_USDC_ADDRESS=0x...

# Phase 2 vault (optional for vault UI)
NEXT_PUBLIC_ECOSTABILIZER_ADDRESS=0x...
NEXT_PUBLIC_SCC_ADDRESS=0x...

# Public API keys
# Prefer using a throwaway/public Alchemy key for client-side
NEXT_PUBLIC_ALCHEMY_API_KEY=
# Optional: WalletConnect (client-exposed)
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=

# IPFS
NEXT_PUBLIC_IPFS_GATEWAY_URL=https://w3s.link/ipfs/

# USDC config
NEXT_PUBLIC_USDC_DECIMALS=6

# Copy to webapp/.env.sepolia and fill values.
