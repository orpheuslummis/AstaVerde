# directories
.coverage_artifacts
.coverage_cache
.coverage_contracts
artifacts
build
cache
coverage
dist
node_modules
types
deployments
tmp

# files
*.env.local
*.log
.DS_Store
.pnp.*
coverage.json
yarn.lock
.vercel
webapp/tsconfig.tsbuildinfo

# Backup files
*.backup
*.bak

# Disabled test files
*.disabled

# Cache directories
.cache-synpress/
*/.cache-synpress/

# Archive directories (local reference)
/archive/

# Duplicate webapp directory
webapp/webapp/

# Temporary test files
webapp/test-wallet-flow.js

# Webapp env files (untracked) — use webapp/.env.local / webapp/.env.*
webapp/.env
webapp/.env.*
!webapp/.env.local.example

# Root Sepolia env (contains secrets) — keep out of git
.env.sepolia
.env.mainnet

# Root Arbitrum env (contains secrets) — keep out of git
.env.arbitrum
.env.arbitrum-sepolia
.env.arbitrum-one

# Root env (contains secrets) — keep out of git
.env

# Ensure Next.js build output is never tracked if created from root context
webapp/.next/

# ESLint reports
eslint-report.json
