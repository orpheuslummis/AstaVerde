# Root Mainnet secrets (do NOT commit)

# Used by scripts and servers; not exposed to client
PRIVATE_KEY=
RPC_API_KEY=

# Optional: block explorer keys (Base)
BASE_MAINNET_EXPLORER_API_KEY=

# Required when deploying a fresh AstaVerde on mainnet
# (Ignored if USE_EXISTING_ASTAVERDE=true and AV_ADDR is set)
USDC_ADDRESS=0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913
OWNER_ADDRESS=0xYourSafe

# Mainnet Phase 2 (vault + SCC) controls
# Set to 'true' to deploy SCC + EcoStabilizer on mainnet
DEPLOY_VAULT_V2=false
# Set to 'true' to use an existing AstaVerde address rather than deploying a new one
USE_EXISTING_ASTAVERDE=false
# Provide when USE_EXISTING_ASTAVERDE=true
AV_ADDR=
# Optional: After granting MINTER_ROLE to the vault, renounce SCC admin
RENOUNCE_SCC_ADMIN=false

# Copy to .env.mainnet locally and keep it out of git.
